# Cognitive Resonance: Architectural Bug Database

This document serves as an immutable ledger of structural bugs, their root causes, and their resolution coordinates across the Cognitive Resonance project. This data is preserved for future architectural analysis and pattern identification.

| ID | Issue & Root Cause | Resolution Strategy | Coordinates (Source / Components) | Session Ref |
|----|-------------------|---------------------|-----------------------------------|-------------|
| **BUG-001** | **`node-pty` Native OS Crash (`posix_spawnp failed`)** <br><br> The native macOS C++ bindings for `node-pty` crashed violently on the `posix_spawnp` syscall when trying to resolve binaries (like `npx` or `/bin/bash`) via the user's Nix profile. | **Refactor Engine.** Entirely cleanly abandoned `node-pty` native extensions and wrapped Node's battle-tested `child_process.spawn({ shell: true })` behind an identical `TerminalInstance` interface. | `packages/terminal-director/src/TerminalInstance.ts` | Current |
| **BUG-002** | **CLI Hardcoded Native I/O Masking Edge Bugs** <br><br> The `cr chat` and `cr serve` loops were hardcoded directly to `process.stdin`, `console.log`, and `setInterval`, making headless programmatic testability impossible and hiding failures in DSL `EXECUTION_REQUESTED` events. | **Dependency Injection.** Decoupled all CLI interactions into interchangeable Adapters (`StdIoAdapter` for prod, `MemoryIoAdapter` for E2E headless clusters). | `packages/core/src/io/IoAdapter.ts`<br>`apps/cli/src/commands/chat.ts`<br>`apps/cli/src/commands/observe.ts` | `ced63c56 / 370081ad` |
| **BUG-003** | **Unsafe Host Invocation via `/exec`** <br><br> The legacy `/exec` command passed raw JS/TS strings via a dangerous native host `exec()` invocation block rather than sandboxing them. | **V8 Isolate Sandboxing.** Replaced host shell delegation with Node's native `vm.runInNewContext()`, matching the exact Cloudflare Worker PWA isolation architecture. | `apps/cli/src/commands/chat.ts` | `380f7ceb` |
| **BUG-004** | **VFS Context Injection Failure** <br><br> The AI assistant (Gemini Service) was operating blindly, failing to receive the imported Virtual File System (VFS) repository contents in its prepended context window prompt. | **Prompt Hydration.** Hooked the REPL interaction loop to actively compute the current VFS boundary tree and dynamically inject it into the AI's system prompt prior to generation. | REPL Context Injection Logic | `370081ad` |
| **BUG-005** | **`TypeError` in File System Promises (E2E)** <br><br> The `GitContextManager` failed asynchronously during session forking tests due to unhandled promise rejections on filesystem I/O operations. | **Promise Chaining Fix.** Hardened the async/await synchronization of filesystem sweeps in `GitContextManager`. | `GitContextManager`<br>`e2e_session_forking.test.ts` | `380f7ceb` |
| **BUG-006** | **`AssertionError` on Fast Session Forking** <br><br> An event count discrepancy occurred inside `e2e_session_forking.test.ts` during a `/clone` operation because the event synchronizer hadn't fully resolved the new node mappings. | **Timing/Assertion Fix.** Hardened the `TestCluster` and `MemoryIoAdapter` async timers to guarantee absolute isolation and event-flush stability before asserting. | `apps/cli/src/__tests__/e2e_session_forking.test.ts` | `380f7ceb` |
| **BUG-007** | **Auth Mock Assertion Deterioration** <br><br> Core authentication tests failed violently because they were asserting against `loginWithEmail` and `signupWithEmail` mock stubs dependent on Appwrite, which had recently been totally stripped out of the PWA. | **Test Suite Alignment.** Updated the `CognitivePlatformContext` test mock payloads to reflect the new local-first and Cloudflare-based Edge architecture expectations. | `packages/core/src/__tests__/CognitivePlatformContext.test.tsx` | `8245f865` |
| **BUG-008** | **Mermaid Syntax Rendering Crash** <br><br> Browser Mermaid renderer crashed when AI-generated code blocks included illegal/unquoted tokens like backticks or spaces inside parentheses. | **Regex Pre-processor.** Updated `MermaidDiagram.tsx` to automatically intercept and securely wrap illegal labels with dynamic double-quotes before rendering. | `MermaidDiagram.tsx` | `e166e262` |
| **BUG-009** | **Share Button Android APK Crash** <br><br> The Capacitor wrapper crashed entirely when attempting to use native Web Share APIs inside the Android build. | **Capacitor Integration.** Resolved native API bridge failures for the Android build target. | Android Native Capabilities | `cddca6a0` |
| **BUG-010** | **Session Parameter Swallowing** <br><br> The `/session ls` parameters (like regex matching) were being incorrectly swallowed and consumed by the broader `/session <id>` parsing logic, breaking session filtering. | **Routing Precision Fix.** Hardened the routing bug to isolate list parameter parsing from explicit ID lookups. | `/session` command parsing | `aaf805b6` |
| **BUG-011** | **PWA 404 Manifest & Routing Error** <br><br> GitHub Pages subdirectory deployments failed to correctly resolve the frontend PWA web manifest and static routes, throwing 404 errors. | **Base Path Configuration.** Corrected the static route bindings and asset paths for the final compilation bundle. | GitHub Pages / PWA routing | `3f485a80` |
| **BUG-012** | **TCAM Ternary Truncation & Display Alignment** <br><br> The system exhibited ternary truncation bugs and key width issues, causing the TCAM display to align incorrectly. | **Visual/Logical Overhaul.** Fixed truncation logic and bounding constraints. | TCAM Display Logic | `bc0d5813` |
| **BUG-013** | **Model Selection UI Bug** <br><br> Variable scoping issues inside a React hook (specifically tracking `modelCount`) caused a regression where the user could no longer physically select a model. | **Hook Variable Scoping.** Fixed the hook dependencies and `modelCount` scoping to restore pointer interaction. | UI Hooks | `8df96b61` |
| **BUG-014** | **Test Mock Drift & False Positives** <br><br> "Shallow" `vi.spyOn(global, 'fetch')` stubs in the `CloudflareAuthProvider` tests drifted from the JWT architecture implementation, causing tests to pass locally while failing E2E. | **Hermetic Network Boundaries (MSW).** Replacing simplistic imperative mocks with Mock Service Worker (MSW) to force tests through actual HTTP/Response protocol channels. | `packages/backend` tests | Current |
| **BUG-015** | **MSW Route Interception Scoping Failure** <br><br> An overarching wildcard MSW interceptor (`*/api/sessions`) accidentally captured a test specifically constructed to hit an invalid domain (`http://invalid.local`), returning a 200 OK and causing the network failure test to falsely succeed. | **Isolated Request Scoping.** Removed the global error-simulation override and bound `server.use(HttpResponse.error())` strictly within the scope of the individual failing test block. | `CloudflareStorageProvider.test.ts` | Current |
| **BUG-016** | **Cross-Workspace ESM Module Resolution Crash** <br><br> `ts-node` executing within a CommonJS workspace (`apps/cli`) crashed with `ERR_UNKNOWN_FILE_EXTENSION` when attempting to recursively load `.ts` dependencies from a native ESM workspace package (`terminal-director`). | **Native Bundler Handover.** Ripped out `ts-node` entirely and delegated module resolution to the modern Node native loader via `NODE_OPTIONS="--import tsx"`. | `apps/cli/cucumber.js` | Current |
